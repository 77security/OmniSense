apiVersion: v1
kind: Service
metadata:
  name: {{ include "omnisense.fullname" . }}
  labels:
    {{- include "omnisense.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "omnisense.selectorLabels" . | nindent 4 }}
